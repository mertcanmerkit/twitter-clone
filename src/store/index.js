import { createStore } from 'vuex';

// Create a new store instance.
const store = createStore({
  state: {
    user: 'asd',
    isAuth: false
  },
  getters: {},
  actions: {},
  mutations: {},
  modules: {}
});

export default store;
